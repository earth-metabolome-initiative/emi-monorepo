//! Submodule defining the connection trait.

use std::error::Error;

/// Trait defining an interface for connections.
pub trait Connection {
    /// Error that may be generated by the connection.
    type Error: Error;
}
