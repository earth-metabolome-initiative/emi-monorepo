//! This module contains the nested structs for the database tables.
//!
//! This file is automatically generated. Do not write anything here.

use std::rc::Rc;

#[derive(PartialEq, Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct NestedOrganismTaxon {
    pub inner: crate::database::flat_variants::OrganismTaxon,
    pub created_by: Rc<crate::database::nested_variants::NestedUser>,
    pub organism: Rc<crate::database::nested_variants::NestedOrganism>,
    pub taxon: Rc<crate::database::nested_variants::NestedTaxon>,
}

unsafe impl Send for NestedOrganismTaxon {}
unsafe impl Sync for NestedOrganismTaxon {}
impl crate::database::Tabular for NestedOrganismTaxon {
    const TABLE: crate::database::Table = crate::database::Table::OrganismTaxa;
}
impl crate::database::Filtrable for NestedOrganismTaxon {
    type Filter = crate::database::filter_variants::OrganismTaxonFilter;
}
impl crate::database::Describable for NestedOrganismTaxon {
    fn description(&self) -> Option<&str> {
        self.inner.description()
    }
}
impl crate::database::Colorable for NestedOrganismTaxon {
    fn color(&self) -> Option<&str> {
        None
    }
}
#[cfg(feature = "frontend")]
impl crate::database::AllRecords for NestedOrganismTaxon {
    fn all_records<C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut>(
        filter: Option<&<Self as crate::database::Filtrable>::Filter>,
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> impl std::future::Future<Output = Result<Vec<Self>, crate::api::ApiError>> {
        Self::all(filter, limit, offset, connection)
    }
}
#[cfg(feature = "frontend")]
impl NestedOrganismTaxon {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_variant` - The flat struct.
    /// * `connection` - The database connection.
    pub(crate) async fn from_flat(
        flat_variant: crate::database::flat_variants::OrganismTaxon,
        connection: &mut gluesql::prelude::Glue<
            impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
        >,
    ) -> Result<Self, crate::api::ApiError> {
        Ok(Self {
            created_by: Rc::from(
                crate::database::nested_variants::NestedUser::get(
                    flat_variant.created_by,
                    connection,
                )
                .await?
                .unwrap(),
            ),
            organism: Rc::from(
                crate::database::nested_variants::NestedOrganism::get(
                    flat_variant.organism_id,
                    connection,
                )
                .await?
                .unwrap(),
            ),
            taxon: Rc::from(
                crate::database::nested_variants::NestedTaxon::get(
                    flat_variant.taxon_id,
                    connection,
                )
                .await?
                .unwrap(),
            ),
            inner: flat_variant,
        })
    }
    /// Get the created_by attribute.
    pub fn get_created_by<E>(&self) -> &E
    where
        i32: AsRef<E>,
    {
        self.inner.get_created_by()
    }
    /// Get the created_at attribute.
    pub fn get_created_at<E>(&self) -> &E
    where
        chrono::NaiveDateTime: AsRef<E>,
    {
        self.inner.get_created_at()
    }
    /// Get the organism_id attribute.
    pub fn get_organism_id<E>(&self) -> &E
    where
        uuid::Uuid: AsRef<E>,
    {
        self.inner.get_organism_id()
    }
    /// Get the taxon_id attribute.
    pub fn get_taxon_id<E>(&self) -> &E
    where
        i32: AsRef<E>,
    {
        self.inner.get_taxon_id()
    }
    /// Get the OrganismTaxon from the database by its ID.
    ///
    /// * `( organism_id, taxon_id )` - The primary key(s) of the struct to check.
    /// * `connection` - The connection to the database.
    pub async fn get<C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut>(
        (organism_id, taxon_id): (uuid::Uuid, i32),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, crate::api::ApiError> {
        if let Some(flat_variant) =
            crate::database::flat_variants::OrganismTaxon::get((organism_id, taxon_id), connection)
                .await?
        {
            Ok(Some(Self::from_flat(flat_variant, connection).await?))
        } else {
            Ok(None)
        }
    }
    /// Delete the OrganismTaxon from the database.
    ///
    /// * `connection` - The connection to the database.
    pub async fn delete<C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<usize, crate::api::ApiError> {
        self.inner.delete(connection).await
    }
    /// Delete the OrganismTaxon from the database by its ID.
    ///
    /// * `( organism_id, taxon_id )` - The primary key(s) of the struct to delete.
    /// * `connection` - The connection to the database.
    pub async fn delete_from_id<
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    >(
        (organism_id, taxon_id): (uuid::Uuid, i32),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<usize, crate::api::ApiError> {
        crate::database::flat_variants::OrganismTaxon::delete_from_id(
            (organism_id, taxon_id),
            connection,
        )
        .await
    }
    /// Get all OrganismTaxon from the database.
    ///
    /// * `filter` - The filter to apply to the results.
    /// * `limit` - The maximum number of results, by default `10`.
    /// * `offset` - The offset of the results, by default `0`.
    /// * `connection` - The connection to the database.
    pub async fn all<C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut>(
        filter: Option<&crate::database::filter_variants::OrganismTaxonFilter>,
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, crate::api::ApiError> {
        let mut organism_taxa = Vec::new();
        for flat_variant in
            crate::database::flat_variants::OrganismTaxon::all(filter, limit, offset, connection)
                .await?
                .into_iter()
        {
            organism_taxa.push(Self::from_flat(flat_variant, connection).await?);
        }
        Ok(organism_taxa)
    }
    /// Update or insert the record in the database.
    ///
    /// * `connection` - The connection to the database.
    pub async fn update_or_insert<
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    >(
        &self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<usize, crate::api::ApiError> {
        crate::database::nested_variants::NestedUser::update_or_insert(
            self.created_by.as_ref(),
            connection,
        )
        .await?;
        crate::database::nested_variants::NestedOrganism::update_or_insert(
            self.organism.as_ref(),
            connection,
        )
        .await?;
        crate::database::nested_variants::NestedTaxon::update_or_insert(
            self.taxon.as_ref(),
            connection,
        )
        .await?;
        crate::database::flat_variants::OrganismTaxon::update_or_insert(&self.inner, connection)
            .await
    }
}
