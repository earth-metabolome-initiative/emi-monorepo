[package]
name = "mermaid"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true, optional = true }
common_traits.workspace = true

[features]
default = ["serde"]
serde = ["dep:serde", "common_traits/serde"]

[lints]
workspace = true
