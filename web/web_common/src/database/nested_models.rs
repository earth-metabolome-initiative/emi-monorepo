//! This module contains the nested structs for the database tables.
//!
//! This file is automatically generated. Do not write anything here.

use serde::Deserialize;
use serde::Serialize;
use uuid::Uuid;
use super::tables::*;
use super::views::*;
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedBioOttRank {
    pub inner: BioOttRank,
    pub font_awesome_icon: FontAwesomeIcon,
}

#[cfg(feature = "frontend")]
impl NestedBioOttRank {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: BioOttRank,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            font_awesome_icon: FontAwesomeIcon::get(flat_struct.font_awesome_icon_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = BioOttRank::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = BioOttRank::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.font_awesome_icon.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedBioOttTaxonItem {
    pub inner: BioOttTaxonItem,
    pub ott_rank: NestedBioOttRank,
    pub domain: Option<BioOttTaxonItem>,
    pub kingdom: Option<BioOttTaxonItem>,
    pub phylum: Option<BioOttTaxonItem>,
    pub class: Option<BioOttTaxonItem>,
    pub order: Option<BioOttTaxonItem>,
    pub family: Option<BioOttTaxonItem>,
    pub genus: Option<BioOttTaxonItem>,
    pub parent: BioOttTaxonItem,
    pub font_awesome_icon: FontAwesomeIcon,
    pub color: Color,
}

#[cfg(feature = "frontend")]
impl NestedBioOttTaxonItem {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: BioOttTaxonItem,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            ott_rank: NestedBioOttRank::get(flat_struct.ott_rank_id, connection).await?.unwrap(),
            domain: if let Some(domain_id) = flat_struct.domain_id { BioOttTaxonItem::get(domain_id, connection).await? } else { None },
            kingdom: if let Some(kingdom_id) = flat_struct.kingdom_id { BioOttTaxonItem::get(kingdom_id, connection).await? } else { None },
            phylum: if let Some(phylum_id) = flat_struct.phylum_id { BioOttTaxonItem::get(phylum_id, connection).await? } else { None },
            class: if let Some(class_id) = flat_struct.class_id { BioOttTaxonItem::get(class_id, connection).await? } else { None },
            order: if let Some(order_id) = flat_struct.order_id { BioOttTaxonItem::get(order_id, connection).await? } else { None },
            family: if let Some(family_id) = flat_struct.family_id { BioOttTaxonItem::get(family_id, connection).await? } else { None },
            genus: if let Some(genus_id) = flat_struct.genus_id { BioOttTaxonItem::get(genus_id, connection).await? } else { None },
            parent: BioOttTaxonItem::get(flat_struct.parent_id, connection).await?.unwrap(),
            font_awesome_icon: FontAwesomeIcon::get(flat_struct.font_awesome_icon_id, connection).await?.unwrap(),
            color: Color::get(flat_struct.color_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = BioOttTaxonItem::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = BioOttTaxonItem::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.ott_rank.update_or_insert(connection).await?;
        if let Some(domain) = self.domain {
            domain.update_or_insert(connection).await?;
        }
        if let Some(kingdom) = self.kingdom {
            kingdom.update_or_insert(connection).await?;
        }
        if let Some(phylum) = self.phylum {
            phylum.update_or_insert(connection).await?;
        }
        if let Some(class) = self.class {
            class.update_or_insert(connection).await?;
        }
        if let Some(order) = self.order {
            order.update_or_insert(connection).await?;
        }
        if let Some(family) = self.family {
            family.update_or_insert(connection).await?;
        }
        if let Some(genus) = self.genus {
            genus.update_or_insert(connection).await?;
        }
        self.parent.update_or_insert(connection).await?;
        self.font_awesome_icon.update_or_insert(connection).await?;
        self.color.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedDerivedSample {
    pub inner: DerivedSample,
    pub created_by: User,
    pub updated_by: User,
    pub parent_sample: NestedSample,
    pub child_sample: NestedSample,
}

#[cfg(feature = "frontend")]
impl NestedDerivedSample {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: DerivedSample,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            parent_sample: NestedSample::get(flat_struct.parent_sample_id, connection).await?.unwrap(),
            child_sample: NestedSample::get(flat_struct.child_sample_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `( parent_sample_id, child_sample_id )` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        ( parent_sample_id, child_sample_id ): ( Uuid, Uuid ),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = DerivedSample::get(( parent_sample_id, child_sample_id ), connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = DerivedSample::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = DerivedSample::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        self.parent_sample.update_or_insert(connection).await?;
        self.child_sample.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedLoginProvider {
    pub inner: LoginProvider,
    pub font_awesome_icon: FontAwesomeIcon,
    pub color: Color,
}

#[cfg(feature = "frontend")]
impl NestedLoginProvider {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: LoginProvider,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            font_awesome_icon: FontAwesomeIcon::get(flat_struct.font_awesome_icon_id, connection).await?.unwrap(),
            color: Color::get(flat_struct.color_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = LoginProvider::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = LoginProvider::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.font_awesome_icon.update_or_insert(connection).await?;
        self.color.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedNotification {
    pub inner: Notification,
    pub user: User,
}

#[cfg(feature = "frontend")]
impl NestedNotification {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: Notification,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            user: User::get(flat_struct.user_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = Notification::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = Notification::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.user.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedOrganization {
    pub inner: Organization,
    pub country: Country,
}

#[cfg(feature = "frontend")]
impl NestedOrganization {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: Organization,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            country: Country::get(flat_struct.country_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = Organization::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = Organization::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.country.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedProjectState {
    pub inner: ProjectState,
    pub font_awesome_icon: FontAwesomeIcon,
    pub color: Color,
}

#[cfg(feature = "frontend")]
impl NestedProjectState {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: ProjectState,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            font_awesome_icon: FontAwesomeIcon::get(flat_struct.font_awesome_icon_id, connection).await?.unwrap(),
            color: Color::get(flat_struct.color_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = ProjectState::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = ProjectState::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.font_awesome_icon.update_or_insert(connection).await?;
        self.color.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedProject {
    pub inner: Project,
    pub state: NestedProjectState,
    pub parent_project: Option<Project>,
    pub created_by: User,
    pub updated_by: User,
}

#[cfg(feature = "frontend")]
impl NestedProject {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: Project,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            state: NestedProjectState::get(flat_struct.state_id, connection).await?.unwrap(),
            parent_project: if let Some(parent_project_id) = flat_struct.parent_project_id { Project::get(parent_project_id, connection).await? } else { None },
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = Project::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = Project::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = Project::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.state.update_or_insert(connection).await?;
        if let Some(parent_project) = self.parent_project {
            parent_project.update_or_insert(connection).await?;
        }
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedProjectsTeamsRole {
    pub inner: ProjectsTeamsRole,
    pub table: NestedProject,
    pub team: NestedTeam,
    pub role: NestedRole,
    pub created_by: User,
    pub updated_by: User,
}

#[cfg(feature = "frontend")]
impl NestedProjectsTeamsRole {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: ProjectsTeamsRole,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            table: NestedProject::get(flat_struct.table_id, connection).await?.unwrap(),
            team: NestedTeam::get(flat_struct.team_id, connection).await?.unwrap(),
            role: NestedRole::get(flat_struct.role_id, connection).await?.unwrap(),
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `( table_id, team_id, role_id )` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        ( table_id, team_id, role_id ): ( i32, i32, i32 ),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = ProjectsTeamsRole::get(( table_id, team_id, role_id ), connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = ProjectsTeamsRole::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = ProjectsTeamsRole::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.table.update_or_insert(connection).await?;
        self.team.update_or_insert(connection).await?;
        self.role.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedProjectsUsersRole {
    pub inner: ProjectsUsersRole,
    pub table: NestedProject,
    pub user: User,
    pub role: NestedRole,
    pub created_by: User,
    pub updated_by: User,
}

#[cfg(feature = "frontend")]
impl NestedProjectsUsersRole {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: ProjectsUsersRole,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            table: NestedProject::get(flat_struct.table_id, connection).await?.unwrap(),
            user: User::get(flat_struct.user_id, connection).await?.unwrap(),
            role: NestedRole::get(flat_struct.role_id, connection).await?.unwrap(),
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `( table_id, user_id, role_id )` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        ( table_id, user_id, role_id ): ( i32, i32, i32 ),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = ProjectsUsersRole::get(( table_id, user_id, role_id ), connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = ProjectsUsersRole::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = ProjectsUsersRole::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.table.update_or_insert(connection).await?;
        self.user.update_or_insert(connection).await?;
        self.role.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedRole {
    pub inner: Role,
    pub font_awesome_icon: FontAwesomeIcon,
    pub color: Color,
}

#[cfg(feature = "frontend")]
impl NestedRole {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: Role,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            font_awesome_icon: FontAwesomeIcon::get(flat_struct.font_awesome_icon_id, connection).await?.unwrap(),
            color: Color::get(flat_struct.color_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = Role::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = Role::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.font_awesome_icon.update_or_insert(connection).await?;
        self.color.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedSampleBioOttTaxonItem {
    pub inner: SampleBioOttTaxonItem,
    pub created_by: User,
    pub updated_by: User,
    pub sample: NestedSample,
    pub taxon: NestedBioOttTaxonItem,
}

#[cfg(feature = "frontend")]
impl NestedSampleBioOttTaxonItem {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: SampleBioOttTaxonItem,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            sample: NestedSample::get(flat_struct.sample_id, connection).await?.unwrap(),
            taxon: NestedBioOttTaxonItem::get(flat_struct.taxon_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `( sample_id, taxon_id )` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        ( sample_id, taxon_id ): ( Uuid, i32 ),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = SampleBioOttTaxonItem::get(( sample_id, taxon_id ), connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SampleBioOttTaxonItem::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SampleBioOttTaxonItem::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        self.sample.update_or_insert(connection).await?;
        self.taxon.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedSampleState {
    pub inner: SampleState,
    pub font_awesome_icon: FontAwesomeIcon,
    pub color: Color,
}

#[cfg(feature = "frontend")]
impl NestedSampleState {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: SampleState,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            font_awesome_icon: FontAwesomeIcon::get(flat_struct.font_awesome_icon_id, connection).await?.unwrap(),
            color: Color::get(flat_struct.color_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = SampleState::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SampleState::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.font_awesome_icon.update_or_insert(connection).await?;
        self.color.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedSampledIndividualBioOttTaxonItem {
    pub inner: SampledIndividualBioOttTaxonItem,
    pub created_by: User,
    pub updated_by: User,
    pub sampled_individual: NestedSampledIndividual,
    pub taxon: NestedBioOttTaxonItem,
}

#[cfg(feature = "frontend")]
impl NestedSampledIndividualBioOttTaxonItem {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: SampledIndividualBioOttTaxonItem,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            sampled_individual: NestedSampledIndividual::get(flat_struct.sampled_individual_id, connection).await?.unwrap(),
            taxon: NestedBioOttTaxonItem::get(flat_struct.taxon_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `( sampled_individual_id, taxon_id )` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        ( sampled_individual_id, taxon_id ): ( Uuid, i32 ),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = SampledIndividualBioOttTaxonItem::get(( sampled_individual_id, taxon_id ), connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SampledIndividualBioOttTaxonItem::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SampledIndividualBioOttTaxonItem::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        self.sampled_individual.update_or_insert(connection).await?;
        self.taxon.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedSampledIndividual {
    pub inner: SampledIndividual,
    pub created_by: User,
    pub updated_by: User,
}

#[cfg(feature = "frontend")]
impl NestedSampledIndividual {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: SampledIndividual,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: Uuid,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = SampledIndividual::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SampledIndividual::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SampledIndividual::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedSampledIndividualsTeamsRole {
    pub inner: SampledIndividualsTeamsRole,
    pub table: NestedSampledIndividual,
    pub team: NestedTeam,
    pub role: NestedRole,
    pub created_by: User,
    pub updated_by: User,
}

#[cfg(feature = "frontend")]
impl NestedSampledIndividualsTeamsRole {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: SampledIndividualsTeamsRole,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            table: NestedSampledIndividual::get(flat_struct.table_id, connection).await?.unwrap(),
            team: NestedTeam::get(flat_struct.team_id, connection).await?.unwrap(),
            role: NestedRole::get(flat_struct.role_id, connection).await?.unwrap(),
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `( table_id, team_id, role_id )` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        ( table_id, team_id, role_id ): ( Uuid, i32, i32 ),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = SampledIndividualsTeamsRole::get(( table_id, team_id, role_id ), connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SampledIndividualsTeamsRole::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SampledIndividualsTeamsRole::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.table.update_or_insert(connection).await?;
        self.team.update_or_insert(connection).await?;
        self.role.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedSampledIndividualsUsersRole {
    pub inner: SampledIndividualsUsersRole,
    pub table: NestedSampledIndividual,
    pub user: User,
    pub role: NestedRole,
    pub created_by: User,
    pub updated_by: User,
}

#[cfg(feature = "frontend")]
impl NestedSampledIndividualsUsersRole {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: SampledIndividualsUsersRole,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            table: NestedSampledIndividual::get(flat_struct.table_id, connection).await?.unwrap(),
            user: User::get(flat_struct.user_id, connection).await?.unwrap(),
            role: NestedRole::get(flat_struct.role_id, connection).await?.unwrap(),
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `( table_id, user_id, role_id )` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        ( table_id, user_id, role_id ): ( Uuid, i32, i32 ),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = SampledIndividualsUsersRole::get(( table_id, user_id, role_id ), connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SampledIndividualsUsersRole::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SampledIndividualsUsersRole::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.table.update_or_insert(connection).await?;
        self.user.update_or_insert(connection).await?;
        self.role.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedSample {
    pub inner: Sample,
    pub created_by: User,
    pub sampled_by: User,
    pub updated_by: User,
    pub state: NestedSampleState,
}

#[cfg(feature = "frontend")]
impl NestedSample {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: Sample,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            sampled_by: User::get(flat_struct.sampled_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            state: NestedSampleState::get(flat_struct.state, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: Uuid,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = Sample::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = Sample::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = Sample::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.sampled_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        self.state.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedSamplesTeamsRole {
    pub inner: SamplesTeamsRole,
    pub table: NestedSample,
    pub team: NestedTeam,
    pub role: NestedRole,
    pub created_by: User,
    pub updated_by: User,
}

#[cfg(feature = "frontend")]
impl NestedSamplesTeamsRole {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: SamplesTeamsRole,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            table: NestedSample::get(flat_struct.table_id, connection).await?.unwrap(),
            team: NestedTeam::get(flat_struct.team_id, connection).await?.unwrap(),
            role: NestedRole::get(flat_struct.role_id, connection).await?.unwrap(),
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `( table_id, team_id, role_id )` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        ( table_id, team_id, role_id ): ( Uuid, i32, i32 ),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = SamplesTeamsRole::get(( table_id, team_id, role_id ), connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SamplesTeamsRole::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SamplesTeamsRole::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.table.update_or_insert(connection).await?;
        self.team.update_or_insert(connection).await?;
        self.role.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedSamplesUsersRole {
    pub inner: SamplesUsersRole,
    pub table: NestedSample,
    pub user: User,
    pub role: NestedRole,
    pub created_by: User,
    pub updated_by: User,
}

#[cfg(feature = "frontend")]
impl NestedSamplesUsersRole {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: SamplesUsersRole,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            table: NestedSample::get(flat_struct.table_id, connection).await?.unwrap(),
            user: User::get(flat_struct.user_id, connection).await?.unwrap(),
            role: NestedRole::get(flat_struct.role_id, connection).await?.unwrap(),
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `( table_id, user_id, role_id )` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        ( table_id, user_id, role_id ): ( Uuid, i32, i32 ),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = SamplesUsersRole::get(( table_id, user_id, role_id ), connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SamplesUsersRole::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SamplesUsersRole::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.table.update_or_insert(connection).await?;
        self.user.update_or_insert(connection).await?;
        self.role.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedSpectra {
    pub inner: Spectra,
    pub spectra_collection: NestedSpectraCollection,
}

#[cfg(feature = "frontend")]
impl NestedSpectra {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: Spectra,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            spectra_collection: NestedSpectraCollection::get(flat_struct.spectra_collection_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = Spectra::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = Spectra::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.spectra_collection.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedSpectraCollection {
    pub inner: SpectraCollection,
    pub sample: NestedSample,
    pub created_by: User,
    pub updated_by: User,
}

#[cfg(feature = "frontend")]
impl NestedSpectraCollection {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: SpectraCollection,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            sample: NestedSample::get(flat_struct.sample_id, connection).await?.unwrap(),
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = SpectraCollection::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SpectraCollection::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SpectraCollection::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.sample.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedSpectraCollectionsTeamsRole {
    pub inner: SpectraCollectionsTeamsRole,
    pub table: NestedSpectraCollection,
    pub team: NestedTeam,
    pub role: NestedRole,
    pub created_by: User,
    pub updated_by: User,
}

#[cfg(feature = "frontend")]
impl NestedSpectraCollectionsTeamsRole {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: SpectraCollectionsTeamsRole,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            table: NestedSpectraCollection::get(flat_struct.table_id, connection).await?.unwrap(),
            team: NestedTeam::get(flat_struct.team_id, connection).await?.unwrap(),
            role: NestedRole::get(flat_struct.role_id, connection).await?.unwrap(),
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `( table_id, team_id, role_id )` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        ( table_id, team_id, role_id ): ( i32, i32, i32 ),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = SpectraCollectionsTeamsRole::get(( table_id, team_id, role_id ), connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SpectraCollectionsTeamsRole::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SpectraCollectionsTeamsRole::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.table.update_or_insert(connection).await?;
        self.team.update_or_insert(connection).await?;
        self.role.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedSpectraCollectionsUsersRole {
    pub inner: SpectraCollectionsUsersRole,
    pub table: NestedSpectraCollection,
    pub user: User,
    pub role: NestedRole,
    pub created_by: User,
    pub updated_by: User,
}

#[cfg(feature = "frontend")]
impl NestedSpectraCollectionsUsersRole {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: SpectraCollectionsUsersRole,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            table: NestedSpectraCollection::get(flat_struct.table_id, connection).await?.unwrap(),
            user: User::get(flat_struct.user_id, connection).await?.unwrap(),
            role: NestedRole::get(flat_struct.role_id, connection).await?.unwrap(),
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `( table_id, user_id, role_id )` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        ( table_id, user_id, role_id ): ( i32, i32, i32 ),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = SpectraCollectionsUsersRole::get(( table_id, user_id, role_id ), connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SpectraCollectionsUsersRole::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = SpectraCollectionsUsersRole::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.table.update_or_insert(connection).await?;
        self.user.update_or_insert(connection).await?;
        self.role.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedTeamState {
    pub inner: TeamState,
    pub font_awesome_icon: FontAwesomeIcon,
    pub color: Color,
}

#[cfg(feature = "frontend")]
impl NestedTeamState {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: TeamState,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            font_awesome_icon: FontAwesomeIcon::get(flat_struct.font_awesome_icon_id, connection).await?.unwrap(),
            color: Color::get(flat_struct.color_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = TeamState::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = TeamState::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.font_awesome_icon.update_or_insert(connection).await?;
        self.color.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedTeam {
    pub inner: Team,
    pub parent_team: Option<Team>,
    pub created_by: User,
    pub updated_by: User,
}

#[cfg(feature = "frontend")]
impl NestedTeam {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: Team,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            parent_team: if let Some(parent_team_id) = flat_struct.parent_team_id { Team::get(parent_team_id, connection).await? } else { None },
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = Team::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = Team::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = Team::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        if let Some(parent_team) = self.parent_team {
            parent_team.update_or_insert(connection).await?;
        }
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedTeamsUsersRole {
    pub inner: TeamsUsersRole,
    pub table: NestedTeam,
    pub user: User,
    pub role: NestedRole,
    pub created_by: User,
    pub updated_by: User,
}

#[cfg(feature = "frontend")]
impl NestedTeamsUsersRole {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: TeamsUsersRole,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            table: NestedTeam::get(flat_struct.table_id, connection).await?.unwrap(),
            user: User::get(flat_struct.user_id, connection).await?.unwrap(),
            role: NestedRole::get(flat_struct.role_id, connection).await?.unwrap(),
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            updated_by: User::get(flat_struct.updated_by, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `( table_id, user_id, role_id )` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        ( table_id, user_id, role_id ): ( i32, i32, i32 ),
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = TeamsUsersRole::get(( table_id, user_id, role_id ), connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = TeamsUsersRole::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Get all the nested structs from the database ordered by the `updated_at` column.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all_by_updated_at<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = TeamsUsersRole::all_by_updated_at(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.table.update_or_insert(connection).await?;
        self.user.update_or_insert(connection).await?;
        self.role.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.updated_by.update_or_insert(connection).await?;
        Ok(())
    }
}
#[derive(Eq, PartialEq, Debug, Clone, Serialize, Deserialize, Default)]
pub struct NestedUserEmail {
    pub inner: UserEmail,
    pub created_by: User,
    pub login_provider: NestedLoginProvider,
}

#[cfg(feature = "frontend")]
impl NestedUserEmail {
    /// Convert the flat struct to the nested struct.
    ///
    /// # Arguments
    /// * `flat_struct` - The flat struct.
    /// * `connection` - The database connection.
    pub async fn from_flat(
        flat_struct: UserEmail,
        connection: &mut gluesql::prelude::Glue<impl gluesql::core::store::GStore + gluesql::core::store::GStoreMut>,
    ) -> Result<Self, gluesql::prelude::Error> {
        Ok(Self {
            created_by: User::get(flat_struct.created_by, connection).await?.unwrap(),
            login_provider: NestedLoginProvider::get(flat_struct.login_provider_id, connection).await?.unwrap(),
            inner: flat_struct,
        })
    }
    /// Get the nested struct from the provided primary key.
    ///
    /// # Arguments
    /// * `id` - The primary key(s) of the row.
    /// * `connection` - The database connection.
    pub async fn get<C>(
        id: i32,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Option<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
       let flat_struct = UserEmail::get(id, connection).await?;        match flat_struct {
            Some(flat_struct) => Ok(Some(Self::from_flat(flat_struct, connection).await?)),
            None => Ok(None),
        }
    }
    /// Get all the nested structs from the database.
    ///
    /// # Arguments
    /// * `limit` - The maximum number of rows to return.
    /// * `offset` - The number of rows to skip.
    /// * `connection` - The database connection.
    pub async fn all<C>(
        limit: Option<i64>,
        offset: Option<i64>,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<Vec<Self>, gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        let flat_structs = UserEmail::all(limit, offset, connection).await?;
         let mut nested_structs = Vec::with_capacity(flat_structs.len());
         for flat_struct in flat_structs {
             nested_structs.push(Self::from_flat(flat_struct, connection).await?);
         }
         Ok(nested_structs)
    }
    /// Update or insert the nested struct into the database.
    ///
    /// # Arguments
    /// * `connection` - The database connection.
    pub async fn update_or_insert<C>(
        self,
        connection: &mut gluesql::prelude::Glue<C>,
    ) -> Result<(), gluesql::prelude::Error> where
        C: gluesql::core::store::GStore + gluesql::core::store::GStoreMut,
    {
        self.inner.update_or_insert(connection).await?;
        self.created_by.update_or_insert(connection).await?;
        self.login_provider.update_or_insert(connection).await?;
        Ok(())
    }
}
