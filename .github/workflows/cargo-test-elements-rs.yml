name: Test

on:
  push:
    paths:
      - 'web/web_common/elements_rs/**'
  pull_request:
    paths:
      - 'web/web_common/elements_rs/**'

jobs:
  test:
    uses: ./.github/workflows/cargo-test-template.yml
    with:
      path: 'web/web_common/elements_rs'
