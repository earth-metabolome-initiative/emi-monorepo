//! Submodule providing the `PgModel` trait which standardizes the properties of
//! diesel model structs representing PostgreSQL system catalog tables.

/// A trait representing a PostgreSQL model in the `pg_diesel` crate.
pub trait PgModel {}
