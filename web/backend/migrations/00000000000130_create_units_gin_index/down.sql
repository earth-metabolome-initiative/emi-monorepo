-- Drop index on the units table.
-- The index was used to run approximate search queries on the table.
-- DROP INDEX units_name_unit_trgm_idx;

-- DROP FUNCTION concat_units_name_unit(name text, description text);
SELECT 1;
